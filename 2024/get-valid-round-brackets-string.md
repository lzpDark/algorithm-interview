####　题目描述

这样定义一个‘正确’的字符串：
1. 只包含  `(` 或者 `)` 两种字符
2. 每个`)`的前面要有匹配的`(`，可以不相邻。

问题1：
判断输入字符是否是‘正确’的

问题2：
输入一个已知‘不正确’的字符串，通过少量修改，返回正确的形式。

如： 
1. `())` -> `()()`
2. `((()` -> `((()))`

#### 代码

借助stack结构，在遇到错误的位置补充上正确的括号

> GetValidRoundBracketsString.java